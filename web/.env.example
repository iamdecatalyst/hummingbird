# Copy to .env.local and fill in real values. Never commit .env.local.

# Nexus OAuth client
VITE_NEXUS_CLIENT_ID=

# Vylth annotator widget token (dev/staging only — do NOT set in production)
# Token for project "hummingbird" — get from ~/.vylth-secrets/annotator-tokens.txt
VITE_ANNOT_TOKEN=
